<h1 class="text-3xl font-semibold mb-6">
  Film lovers, critics and friends — find popular members.
</h1>

<ul class="space-y-4">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user_path(user.username), class: "link" %>
    </li>
  <% end %>
</ul>
